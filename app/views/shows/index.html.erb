     <div class="row">
        <div class="large-12 columns">
            <h1>Shows</h1>
            <ul class="small-block-grid-2 medium-block-grid-3 large-block-grid-5">
                
    <% @shows.each do |show| %>
            <li>
                <% poster = show.banners.where("banner_type = 'poster'").first %>
                <%=  %>
                <%= link_to(image_tag("http://thetvdb.com/banners/" + poster.image_path), show) %>
            </li>
      <% end %>
            </ul>
        </div>
    </div>
<br>



<%= link_to 'New Show', new_show_path %>


<%= link_to "Update all shows", update_all_shows_path %>
