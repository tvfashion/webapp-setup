<h1>Listing seasons</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Image</th>
      <th>Show</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @seasons.each do |season| %>
      <tr>
        <td><%= season.name %></td>
        <td><%= season.image %></td>
        <td><%= season.show_id %></td>
        <td><%= link_to 'Show', show_season_path(@show, season) %></td>
        <td><%= link_to 'Edit', edit_show_season_path(@show, season) %></td>
        <td><%= link_to 'Destroy', show_season_path(@show, season), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Season', new_show_season_path %>
